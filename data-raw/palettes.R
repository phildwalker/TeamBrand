## code to prepare `palettes` dataset goes here

# base colors ------------------------------------------------------------

tanger_dark_internal = "#BD2326"

usethis::use_data(tanger_dark_internal, overwrite = TRUE)



# palettes ----------------------------------------------------------------

tanger_colors <-c(
  red = "#BD2326",
  black = "#000000",
  grey = "#464646"
)

usethis::use_data(tanger_colors, overwrite = TRUE)

tanger_colors_pal1 <- c(
  "#BD2326","#000000","#464646"
                        )
usethis::use_data(tanger_colors_pal1, overwrite = TRUE)
